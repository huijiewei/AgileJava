spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:config/
eureka:
   client:
     serviceUrl:
      defaultZone: https://eureka.agile.huijiewei.com/eureka/
