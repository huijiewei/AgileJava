server:
  port: 8089

spring:
  application:
    name: cloud-auth
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: server-config
eureka:
    client:
      serviceUrl:
       defaultZone: http://agile.dev:8082/eureka/