server:
  port: 9091

spring:
  application:
    name: cloud-order
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: server-config
eureka:
    client:
      serviceUrl:
       defaultZone: http://agile.dev:8082/eureka/