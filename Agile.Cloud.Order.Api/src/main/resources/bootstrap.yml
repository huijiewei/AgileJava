server:
  port: 8091

spring:
  application:
    name: cloud-order
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: server-config
eureka:
    client:
      serviceUrl:
       defaultZone: http://discovery.agile.dev:8082/eureka/