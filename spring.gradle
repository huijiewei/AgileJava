apply plugin: 'org.springframework.boot'

ext.springCloudDependenciesVersion = 'Edgware.RELEASE'

dependencyManagement {
    imports {
        mavenBom "org.springframework.cloud:spring-cloud-dependencies:${springCloudDependenciesVersion}"
    }
}